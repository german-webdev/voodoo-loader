import { Input } from "./Input";

const meta = {
  title: "Shared/Input",
  component: Input,
  args: {
    placeholder: "Paste direct URL here",
    value: "",
    onChange: () => undefined,
  },
};

export default meta;

export const Default = {};
